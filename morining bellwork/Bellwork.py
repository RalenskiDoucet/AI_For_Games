'''In python create an application that will find all items in a list
that are less than the value the user specifies. These items should
be added to new list and then the list should be printed to the
screen.'''
class mList(object):
    def __init__(self, m_list):
        self.m_list = m_list
    def find_items(self, m_list):
        for x in range(10, self.m_list):
            new_list=mList(m_list())
            self.m_list.append(new_list)
            if x < m_list[raw_input("")]
            print "items_found"new_list
            elif x > m_list[raw_input("")]:
            print "no_item_found"
    mList = __init__(self, m_list):
